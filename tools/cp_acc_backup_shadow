#!/bin/bash
# Description: cPanel Account Backup Shadow
# Arguments: --domain=DOMAIN
set -e

_tool _cp
_domain ${DOMAIN}
local ACCOUNT=${account}

PATH_SHADOW=/home/${ACCOUNT}/etc/${DOMAIN}/shadow

_copy ${PATH_SHADOW}
